# Local dev: leave unset — Vite proxies /api to http://localhost:8000
# Production (Vercel): set in .env.production or Vercel Environment Variables
VITE_API_URL=https://restaurant-recommendation-app-2deployrecommendation.streamlit.app
